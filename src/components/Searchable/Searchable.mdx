---
name: Searchable
menu: Componentes
---

import { Playground, Props } from 'docz';
import ContentContainer from '../ContentContainer/index.tsx';
import Searchable from './index.tsx';

# Searchable

O Searchable é um componente para ser utilizado preferencialmente junto com o PageHeader.

Ele contém um ícone que indica uma busca, que ao ser clicado expande para um input.

<Props of={Searchable} />

## Uso básico

<Playground>
  {() => {
    const frutas = [
      "pêra",
      "uva",
      "maçã",
      "banana",
      "morango",
    ]

    const [listaDeFrutas, setListaDeFrutas] = useState(frutas)

    const handleSearch = (query) => {
        const novaListaDeFrutas = frutas.filter(fruta => fruta.includes(query))
        setListaDeFrutas(novaListaDeFrutas)
    };
    return (
      <ContentContainer>
        <Searchable onSearch={onChange} />
        <div>
          <ul>
            {listaDeFrutas.map(fruta => <li>{fruta}</li>)}
          </ul>
        </div>
      </ContentContainer>
    );
  }}
</Playground>
